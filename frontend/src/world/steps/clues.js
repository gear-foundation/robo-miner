// Clue blocks / predecessors: pyrite traces before a gold pocket, ancient
// bricks before a vault, glowing stones before a crystal zone, etc.
//
// Purpose: give the player a "the world is speaking" moment — a hint on the
// radar that there's something meaningful behind a barrier.
//
// Stub for MVP-0.

export function placeClues(/* grid, rnd, ctx */) {
  // no-op
}
